en:
  settings:
    images:
      tag_name: Tag Name
      reset_time: Time since last Reset
      edit: Edit
      reset: Reset
      destroy: Destroy
      edit: Edit Portfolio
      reset_cache: 'Reset Cache for %{tag_name}'
      destroy_cache: 'Destroy Cache for %{tag_name}'
      reset_caches: Reset all Caches
      destroy_caches: Destroy all Caches
      edit_portfolio: 'Edit %{tag_name}'
